export default function Home() {
  return (
    <main style={{ minHeight: "100vh", display: "grid", placeItems: "center", background: "#0b1020", color: "#fff" }}>
      <section style={{ textAlign: "center" }}>
        <h1 style={{ fontSize: "2.5rem", marginBottom: "0.5rem" }}>Aviator</h1>
        <p style={{ opacity: 0.8 }}>Le projet est maintenant créé dans le dossier <code>/vercel/sandbox/Aviator</code>.</p>
      </section>
    </main>
  );
}
