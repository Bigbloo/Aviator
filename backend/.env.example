# Backend environment variables — copy to .env and fill in your own keys
PORT=4000
NODE_ENV=production

# Stripe (only needed for real payments — NOT used in demo mode)
STRIPE_SECRET_KEY=sk_test_your_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here
